export interface Product {
  id: string;
  name: string;
  description: string;
  productionDate: string;
  expiryDate: string;
  batchNumber: string;
  supplierId: string;
  createdAt?: string;
  updatedAt?: string;
} 